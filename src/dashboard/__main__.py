"""Entry point: ``python -m src.dashboard`` launches the live dashboard."""

from __future__ import annotations

from src.dashboard.app import run

if __name__ == "__main__":  # pragma: no cover
    run()
